# Servidor
servidor.porta=8080

# Perfis
spring.profiles.active=dev

# AWS Cognito
aws.cognito.userPoolId=sa-east-1_beCUh1uj
aws.region=sa-east-1
aws.cognito.clientId=5f5flgtvn74fu9hqjdg1jhdvcp
aws.cognito.issuer= https://cognito-idp.your-region.amazonaws.com/your-user-pool-id
aws.accessKeyId=seu-id-de-chave-de-acesso
aws.secretKey=sua-chave-secreta

# Banco de dados (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/t6devs_db
spring.datasource.username=postgres
spring.datasource.password=
spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuração do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JPA
spring.jpa.hibernate.ddl-auto=atualização
spring.jpa.show-sql=true

# Registro
logging.level.org.springframework=INFO
logging.level.com.example.t6devsback=DEBUG

# Springdoc (Swagger)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Configurações adicionais
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true